<div class="breadcrumb">
	<p>
	{% if page.breadcrumb %}
		go back to: 
	{% endif %}
	{% for crumb in page.breadcrumb %}
		<a href="{{crumb.url}}" title="{{crumb.title}}">{{crumb.text}}&nbsp;</a>
	{% endfor %}
	</p>
</div>